package heap;

public class k_closest_points_to_origin {
    public static void main(String[] args) {
        // take 2d array of points . And take the pair of distance from origin and the
        // original point into the max heap.Now get the k points and be done with it.
    }
}
